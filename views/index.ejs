<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8" />
    <title>Home - TODO App</title>

    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: "Segoe UI", Arial, sans-serif;
            background: #f3f4f6;
            overflow-x: hidden;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        /* ---------------- HERO SECTION ---------------- */
        .hero {
            min-height: 70vh;
            padding: 35px 20px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            background: linear-gradient(to right, #eef2ff, #f8fafc);
            text-align: center;
        }

        .hero h1 {
            font-size: 30px;
            font-weight: 700;
            color: #1e293b;
            margin-bottom: 12px;
            line-height: 1.2;
        }

        .hero p {
            font-size: 15px;
            color: #475569;
            width: 93%;
            max-width: 420px;
            margin: 0 auto;
            line-height: 1.5;
        }

        .btn {
            display: inline-block;
            padding: 12px 22px;
            background: #2563eb;
            color: white;
            border-radius: 8px;
            margin-top: 22px;
            text-decoration: none;
            font-size: 15.5px;
            font-weight: 600;
            box-shadow: 0 2px 6px rgba(0, 0, 0, 0.15);
            transition: 0.25s ease;
        }

        .btn:hover {
            background: #1d4ed8;
            transform: translateY(-2px);
        }

        /* ---------------- FEATURES SECTION ---------------- */
        .features {
            padding: 35px 20px;
            text-align: center;
        }

        .features h2 {
            font-size: 24px;
            color: #1e293b;
            margin-bottom: 25px;
            font-weight: 700;
        }

        .feature-container {
            display: flex;
            flex-direction: column;
            align-items: center;
            gap: 18px;
        }

        .box {
            width: 95%;
            max-width: 380px;
            background: white;
            padding: 18px 16px;
            border-radius: 12px;
            box-shadow: 0 4px 14px rgba(0, 0, 0, 0.08);
            transition: 0.25s ease;
        }

        .box:hover {
            transform: translateY(-4px);
        }

        .box h3 {
            font-size: 17px;
            margin-bottom: 8px;
            color: #1e293b;
            font-weight: 600;
        }

        .box p {
            color: #64748b;
            font-size: 14px;
            line-height: 1.4;
        }

        /* ---------------- FOOTER ---------------- */
        .footer {
            padding: 15px;
            text-align: center;
            background: #1e293b;
            color: white;
            margin-top: 25px;
            font-size: 13px;
            letter-spacing: 0.2px;
        }

        /* ---------------- Large Screens (Tablet & Desktop) ---------------- */
        @media (min-width: 768px) {
            .hero h1 {
                font-size: 100px;
            }

            .hero p {
                font-size: 19px;
                max-width: 650px;
                line-height: 1.7;
            }

            .feature-container {
                flex-direction: row;
                flex-wrap: wrap;
                justify-content: center;
                gap: 30px;
            }

            .btn {
                font-size: 40px;
                white-space: nowrap;
                padding: 10px 32px;
            }

            .box {
                width: 260px;
            }

            .feature-container {
                flex-direction: row;
                flex-wrap: wrap;
                gap: 35px;
                justify-content: center;
            }

            .box {
                width: 344px;
                /* 🔥 bigger desktop cards */
                padding: 24px;
            }

            .box h3 {
                font-size: 22px;
            }

            .box p {
                font-size: 16px;
                line-height: 1.7;
            }
            
        }
        
        
    </style>



</head>

<body>

    <!-- NAVBAR -->
    <%- include('partials/nav') %>

        <!-- HERO SECTION -->
        <div class="hero">
            <h1>Welcome to TODO List</h1>
            <p>The simplest, smartest way to manage your daily tasks. Increase your productivity, stay organized, and
                accomplish more every day with ease.</p>
            <% if (!user) { %>
                <a class="btn" href="/login">Get Started</a>
                <% } %>
                    <% if (user) { %>
                        <a class="btn" href="/task">View Your Tasks</a>
                        <% } %>
        </div>

        <!-- FEATURES -->
        <div class="features">
            <h2>Key Features</h2>

            <div class="feature-container">

                <div class="box">
                    <h3>➕ Add Tasks</h3>
                    <p>Create tasks quickly and keep track of everything important.</p>
                </div>

                <div class="box">
                    <h3>✔️ Mark Completed</h3>
                    <p>Stay motivated by checking off completed tasks.</p>
                </div>

                <div class="box">
                    <h3>🗑️ Delete Tasks</h3>
                    <p>Remove unwanted tasks to stay clean and organized.</p>
                </div>

                <div class="box">
                    <h3>👤 User Roles</h3>
                    <p>Admin can manage all tasks. Users can manage their own tasks.</p>
                </div>

            </div>
        </div>

        <!-- FOOTER -->
        <div class="footer">
            © 2025 TODO App — All Rights Reserved
        </div>

</body>

</html>
