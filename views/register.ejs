<!DOCTYPE html>
<html>

<head>
    <title>Register</title>

    <style>
        body {
            margin: 0;
            padding: 0;
            background: #f4f4f4;
            font-family: Arial, sans-serif;

            display: flex;
            justify-content: center;
            align-items: center;

            min-height: 100vh;
            /* FIX: replaces height:100vh */
            padding-top: 20px;
            /* FIX: avoids top-white-gap */
            padding-bottom: 20px;
            /* FIX: avoids bottom-white-gap */
        }


        /* BIGGER REGISTER CARD */
        .card {
            width: 90%;
            max-width: 460px;
            /* 🔥 bigger card */
            background: white;
            padding: 40px;
            border-radius: 14px;
            box-shadow: 0px 4px 20px rgba(0, 0, 0, 0.15);
        }

        /* BIG HEADING */
        h2 {
            text-align: center;
            margin-bottom: 25px;
            font-size: 34px;
            /* 🔥 big heading */
        }

        /* BIGGER INPUTS & SELECT */
        input,
        select {
            width: 100%;
            padding: 18px;
            /* 🔥 bigger touch area */
            margin: 16px 0;
            border-radius: 10px;
            border: 1px solid #bdbdbd;
            font-size: 20px;
            /* 🔥 big input text */
            box-sizing: border-box;
        }

        /* BIG LABELS */
        label {
            font-size: 20px;
            font-weight: 700;
        }

        /* BIG BUTTON */
        button {
            width: 100%;
            padding: 18px;
            /* 🔥 bigger button */
            background: #28a745;
            color: white;
            border: none;
            border-radius: 10px;
            cursor: pointer;
            margin-top: 20px;
            font-size: 22px;
            /* 🔥 bigger button text */
            box-sizing: border-box;
        }

        button:hover {
            background: #1f7c35;
        }

        /* ERROR MESSAGE BIG */
        .error {
            color: red;
            text-align: center;
            margin-bottom: 15px;
            font-weight: bold;
            font-size: 18px;
        }

        /* BIG FOOTER LINK */
        .link {
            text-align: center;
            margin-top: 15px;
            font-size: 18px;
        }

        .link a {
            font-size: 20px;
            text-decoration: none;
            color: #2563eb;
            font-weight: 600;
        }

        /* EXTRA LARGE UI ON SMALL MOBILE */
        @media (max-width: 450px) {

            .card {
                padding: 32px 25px;
            }

            h2 {
                font-size: 32px;
            }

            input,
            select {
                padding: 20px;
                font-size: 22px;
            }

            label {
                font-size: 22px;
            }

            button {
                padding: 20px;
                font-size: 24px;
            }

            .link {
                font-size: 20px;
            }

            .link a {
                font-size: 22px;
            }
        }
    </style>
</head>

<body>

    <div class="card">
        <h2>Register</h2>

        <% if (typeof error !=="undefined" ) { %>
            <p class="error">
                <%= error %>
            </p>
            <% } %>


                <form action="/api/v1/user/register" method="POST">

                    <label>Name</label>
                    <input type="text" name="name" required>

                    <label>Email</label>
                    <input type="email" name="email" required>

                    <label>Password</label>
                    <input type="password" name="password" required>

                    <label>Role</label>
                    <select name="role" required>
                        <option value="user">User</option>
                        <option value="admin">Admin</option>
                    </select>

                    <button type="submit">Register</button>
                </form>

                <p class="link">
                    Already have an account? <a href="/login">Login</a>
                </p>
    </div>

</body>

</html>
