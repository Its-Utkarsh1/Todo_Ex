<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <title>Login</title>

  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f4f4f4;
      margin: 0;
      padding: 0;

      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
    }

    /* Bigger login box for mobile */
    .login-box {
      background: white;
      padding: 40px;
      border-radius: 50px;
      width: 90%;
      /* More flexible on mobile */
      max-width: 420px;
      /* Bigger than before */
      box-shadow: 0 4px 18px rgba(0, 0, 0, 0.12);
    }

    /* Bigger heading */
    .login-box h2 {
      font-size: 30px;
      /* Bigger */
      margin-bottom: 20px;
      text-align: center;
    }

    /* Bigger label */
    label {
      font-size: 18px;
      font-weight: 600;
    }

    /* Bigger inputs */
    input {
      width: 100%;
      padding: 15px;
      /* Bigger tap area */
      margin: 14px 0;
      border: 1px solid #ccc;
      border-radius: 8px;
      font-size: 17px;
      /* Bigger text */
      box-sizing: border-box;
    }

    /* Links no underline */
    a {
      text-decoration: none;
      color: #2563eb;
      font-size: 16px;
      font-weight: 600;
    }

    /* Bigger button */
    button {
      width: 100%;
      padding: 15px;
      background: #333;
      color: white;
      border: none;
      cursor: pointer;
      font-size: 18px;
      /* Bigger text */
      border-radius: 8px;
      margin-top: 4px;
    }

    button:hover {
      background: #555;
    }

    /* Improve small mobile screens */
    @media (max-width: 450px) {
  
  .login-box {
    padding: 40px 25px;
  }

  .login-box h2 {
    font-size: 34px;
  }

  input {
    padding: 22px;
    font-size: 22px;
  }

  button {
    padding: 22px;
    font-size: 24px;
  }

  label {
    font-size: 24px;
  }

  a {
    font-size: 22px;
  }
}
  </style>

</head>

<body>

  <div class="login-box">
    <h2>Login</h2>

    <% if (typeof error !=="undefined" ) { %>
      <p style="color:red;">
        <%= error %>
      </p>
      <% } %>

        <form action="/api/v1/user/login" method="POST">
          <label>Email</label>
          <input type="email" name="email" required>

          <label>Password</label>
          <input type="password" name="password" required>

          <button type="submit">Login</button>
        </form>

        <p style="margin-top:15px;">
          Don't have an account? <a href="/register">Register</a>
        </p>
  </div>

</body>

</html>
